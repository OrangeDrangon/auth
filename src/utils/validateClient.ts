export async function validateClient(
  clientId?: string,
  callbackUrl?: string
): Promise<boolean> {
  // validate db for the client id and check if it has that callback url registered to it.
  return true;
}
